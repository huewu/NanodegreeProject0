<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/color_primary</item>
        <item name="colorPrimaryDark">@color/color_primary_dark</item>
        <item name="colorAccent">@color/color_accent</item>
    </style>

    <style name="AppTheme.RedButton" parent="AppTheme">
        <item name="colorButtonNormal">@color/color_primary</item>
    </style>

    <style name="AppWidget.Button" parent="Base.Widget.AppCompat.Button">
        <item name="android:layout_gravity">center_horizontal</item>
        <item name="android:maxWidth">@dimen/max_button_width</item>
        <item name="android:minHeight">@dimen/min_button_height</item>
        <item name="android:layout_marginLeft">@dimen/button_horizontal_margin</item>
        <item name="android:layout_marginRight">@dimen/button_horizontal_margin</item>
        <item name="android:layout_marginTop">@dimen/button_vertical_margin</item>
        <item name="android:layout_marginBottom">@dimen/button_vertical_margin</item>
    </style>

    <style name="AppWidget.Button.Red" parent="AppWidget.Button">
        <item name="android:theme">@style/AppTheme.RedButton</item>
        <item name="android:textColor">@android:color/white</item>
    </style>

</resources>
